=== SUPERSEDED ===

This paste-blob has been replaced by the `.autopilot/` layout.

To advance one iteration of the loop, read:

  .autopilot/PROMPT.md    — canonical prompt (submit verbatim to the agent)
  .autopilot/STATE.md     — live iteration state
  .autopilot/MVP-GATES.md — observable completion scorecard

Operator dashboard (read this first each day):

  .autopilot/대시보드.md

Install hooks (one-time, from repo root):

  .autopilot/project.ps1 install-hooks

The previous stub referenced `prototypes/relay-app-mvp/.autopilot/RUN.txt`,
which never existed in this repo layout. That pointer is removed.

Historical context is preserved in git history and .autopilot/HISTORY.md.
